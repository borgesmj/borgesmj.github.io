---

// Importar plantilla base
import BaseLayout from "../layouts/BaseLayout.astro";
// Componentes
import Header from "../Components/Profile/Header";
import ProfileInfo from "../Components/Profile/ProfileInfo"
import ProfileNav from "../Components/Profile/ProfileNav"
// constantes e la pagina
const pageTitle = "MB | Web Developer";
const pageDescription =
  "Personal portfolio showcasing web development projects with an integrated blog. Utilizes HTML, CSS, and JavaScript for frontend, Git for version control, and continuous deployment. Optimized for performance and user experience. Follow for updates";
const pageUrl = "https://borgesmj.github.io/";
const pageImg = "https://avatars.githubusercontent.com/u/121818423?v=4";
const twitterCardUrl = "@https://borgesmj.github.io/";
// realizar fetch de github
const response = await fetch("https://api.github.com/users/borgesmj");
const data = await response.json();
---

<BaseLayout
  pageTitle={pageTitle}
  pageDescription={pageDescription}
  pageUrl={pageUrl}
  pageImg={pageImg}
  twitterCardUrl={twitterCardUrl}
>
  <div class="flex flex-col justify-center w-full items-center pb-4">
    <!-- <img
      src="https://avatars.githubusercontent.com/u/121818423?v=4"
      alt="Github avatar"
      class="w-40 h-auto"
    />
    <h1
      class="text-primary-100 font-bold text-[2rem] lg:text-[3rem] text-center my-4"
    >
      HI! I'm Miguel 👋
    </h1>
    <p class="p-4 text-center">
      {data.bio}
    </p>
    <div
      class="w-full px-4 flex flex-row justify-between md:w-4/5 md:flex md:justify-between md:items-center"
    >
      <a
        target="_blank"
        href={data?.followers_url}
        class="lg:hover:underline text-center"
        >{`${data.followers} followers`}</a
      >
      <a
        target="_blank"
        href={data?.following_url}
        class="lg:hover:underline text-center"
        >{`${data.following} following`}</a
      >
      <a
        target="_blank"
        href={data?.repos_url}
        class="lg:hover:underline text-center">{`${data.public_repos} repos`}</a
      >
      <a
        target="_blank"
        href={data?.gists_url}
        class="lg:hover:underline text-center">{`${data.public_gists} gists`}</a
      > -->
    <!-- </div> -->
    <Header/>
    <ProfileInfo
      bio = {data.bio}
      location = {data.location}
    />
    <ProfileNav/>
  </div>
</BaseLayout>
